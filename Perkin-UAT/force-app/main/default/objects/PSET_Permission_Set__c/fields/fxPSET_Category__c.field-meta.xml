<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>fxPSET_Category__c</fullName>
    <description>This captures the Standardized Naming Convention and uses it as the category</description>
    <externalId>false</externalId>
    <formula>IF(CONTAINS(Label__c,&quot;: &quot;)
    ,  UPPER(TRIM(LEFT( Label__c , FIND(&quot;:&quot;,Label__c)-1)))
    ,IF( IsOwnedByProfile__c =true
        ,IF( IsCustom__c =false
            ,&quot;Profile-sfdc&quot;
            ,&quot;Profile-custom&quot;
           )
        ,&#39;Non-Std&#39;
        )
  )</formula>
    <inlineHelpText>This captures the Standardized Naming Convention and uses it as the category</inlineHelpText>
    <label>fxPSET Category</label>
    <required>false</required>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
