<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>flagForUpdate__c</fullName>
    <defaultValue>false</defaultValue>
    <description>This is for DEV use only when needing to trigger a Edit on an account so that other things run.</description>
    <inlineHelpText>This is for DEV use only when needing to trigger a Edit on an account so that other things run.</inlineHelpText>
    <label>flagForUpdate</label>
    <trackFeedHistory>false</trackFeedHistory>
    <trackHistory>false</trackHistory>
    <type>Checkbox</type>
</CustomField>
